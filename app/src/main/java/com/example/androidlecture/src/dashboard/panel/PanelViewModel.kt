package com.example.androidlecture.src.dashboard.panel

import androidx.lifecycle.ViewModel
import com.example.androidlecture.src.dashboard.DashboardRepository
import javax.inject.Inject

class PanelViewModel @Inject constructor(dashboardRepository: DashboardRepository): ViewModel() {
}